# Як влаштований RAG

Асистент не «знає» продукцію DEMAX — він щоразу шукає її в базі знань і відповідає
лише знайденим. Нижче — весь шлях одного запитання, з реальними числами з коду.

| Показник | Значення | Де в коді |
|---|---|---|
| Фрагментів у базі знань | 319 | — |
| Розмірність вектора | 384 | `db.py: EMBED_DIM` |
| Кандидатів на гілку пошуку | 12 | `rag.py: CANDIDATES` |
| Фрагментів у контекст моделі | 5 | `rag.py: TOP_K` |
| Розмір фрагмента / перекриття | 1100 / 150 символів | `ingest.py: CHUNK_SIZE` |
| Поріг ескалації (косинус) | 0.30 | `rag.py: ESCALATE_THRESHOLD` |

---

## 1. Загальний вигляд: один оберт

Косметолог пише запитання у віджет і за півтори–три секунди отримує відповідь,
під нею — джерела й, коли доречно, картки товарів. Між цими моментами система
робить тринадцять кроків, і назовні виходить лише один із них.

```mermaid
sequenceDiagram
    autonumber
    participant K as Косметолог
    participant W as Віджет
    participant A as RAG API
    participant D as PostgreSQL
    participant L as LLM-провайдер

    K->>W: запитання + мова інтерфейсу
    W->>A: POST /v1/me/conversations/messages
    A->>A: ембедінг запиту (локально, ONNX)
    A->>D: векторний пошук + повнотекстовий
    D-->>A: 12 + 12 кандидатів
    A->>A: злиття RRF → 5 фрагментів
    A->>D: підбір товарів (триграми + вектор)
    D-->>A: до 3 карток
    A->>L: промпт мовою відповіді + контекст
    L-->>A: текст лише з наданого
    A->>D: збереження репліки в історію
    A-->>W: відповідь + джерела + товари
    W-->>K: текст, картки, посилання
```

---

## 2. Індексація: звідки береться база знань

База збирається автоматично — обхід карти сайту demax.com.ua плюс каталог у PDF.
Перекриття між фрагментами потрібне, щоб думка, розірвана між двома фрагментами,
не загубилася.

```mermaid
flowchart LR
    subgraph S["Джерела"]
        direction TB
        S1["Сайт demax.com.ua<br/>36 сторінок"]
        S2["Каталог продукції<br/>43 товари"]
        S3["Професійний розділ<br/>14 матеріалів"]
        S4["Каталог PDF 2021<br/>1 документ"]
    end

    subgraph P["Обробка"]
        direction TB
        P1["Витяг тексту<br/>очищення розмітки"]
        P2["Нарізка на фрагменти<br/>1100 симв. / 150 перекриття"]
        P3["Ембедінг ONNX<br/>MiniLM · 384 виміри"]
    end

    subgraph B["PostgreSQL 16"]
        direction TB
        B1[("knowledge_articles<br/>заголовок, фото, ціна")]
        B2[("knowledge_chunks<br/>текст + вектор")]
        B3["Індекс HNSW<br/>cosine"]
    end

    S1 --> P1
    S2 --> P1
    S3 --> P1
    S4 --> P1
    P1 --> P2 --> P3
    P3 --> B2
    P1 --> B1
    B2 --> B3
```

> **Чому ембедінги локальні.** Це не оптимізація вартості, а межа периметра.
> Якби вектори рахував зовнішній сервіс, туди довелося б відправити всю базу знань
> цілком. Модель MiniLM працює на тому ж сервері — база не залишає його жодного разу.

**Що саме працює локально.** `embeddings.py` використовує `fastembed` (ONNX Runtime, CPU),
модель `paraphrase-multilingual-MiniLM-L12-v2` у квантованій ONNX-збірці, 384 виміри,
покриває uk/ru/en. Файли моделі (≈240 МБ) **завантажуються один раз** при першому
старті сервісу й лежать у `demax-rag/.models`; після цього кожен ембедінг рахується
на сервері без жодного мережевого виклику. Розмірність вектора зафіксована в схемі БД
(`db.py: EMBED_DIM = 384`) — колонка `knowledge_chunks.embedding` має тип `vector(384)`.

> ⚠️ У продакшені модель ембедінгів — відкрите питання специфікації (OQ-7).
> Поточна MiniLM це демо-заміна. Ми виміряли її проти `bge-m3` на реальних
> питаннях — результати нижче, у розділі 7.

---

## 3. Гібридний пошук і злиття RRF

Векторний пошук розуміє зміст: на «зволоження» він знайде фрагмент про гіалуронову
кислоту, навіть якщо цього слова там немає. Але він губиться на точних назвах.
Повнотекстовий — навпаки. Тому працюють обидва, а списки зводяться методом
**RRF** (Reciprocal Rank Fusion): фрагмент отримує `1 / (60 + позиція)` балів від
кожного списку. Той, що потрапив в обидва, майже завжди обходить лідера будь-якого одного.

```mermaid
flowchart TB
    Q["Запитання косметолога"]

    Q --> E["Ембедінг запиту<br/>384 виміри"]
    Q --> T["Значущі слова через АБО<br/>to_tsquery"]

    subgraph V["Гілка змісту"]
        E --> V1["pgvector · косинус<br/>індекс HNSW"]
        V1 --> V2["12 кандидатів<br/>за близькістю змісту"]
    end

    subgraph F["Гілка точних слів"]
        T --> F1["Повнотекстовий пошук<br/>ts_rank"]
        F1 --> F2["12 кандидатів<br/>за збігом слів"]
    end

    V2 --> R{{"RRF<br/>бал = 1 / (60 + позиція)"}}
    F2 --> R
    R --> TOP["Топ-5 фрагментів<br/>у контекст моделі"]
```

**Як RRF обирає переможця** — розрахунок для запиту «крем для сухої шкіри»:

| Фрагмент | Позиція за змістом | Позиція за словами | Бал гілки 1 | Бал гілки 2 | Разом |
|---|---:|---:|---:|---:|---:|
| **Живильний крем — опис** | 2 | 3 | 0.0161 | 0.0159 | **0.0320** |
| Сторінка категорії «Догляд» | 1 | — | 0.0164 | 0 | 0.0164 |
| Каталог PDF, розділ кремів | — | 1 | 0 | 0.0164 | 0.0164 |
| Стаття про типи шкіри | 4 | — | 0.0156 | 0 | 0.0156 |

Перший рядок не був найкращим у жодному зі списків — і все одно виграв, бо його
підтвердили обидва методи. У цьому суть RRF: згода двох різних пошуків важить
більше, ніж упевненість одного. *(У коді позиції нумеруються з нуля — на порядок це не впливає.)*

---

## 4. Рішення: відповісти чи передати менеджеру

Найризикованіше, що може зробити консультант бренду, — впевнено вигадати факт.
Тому перед генерацією стоять три фільтри, і два з них працюють у коді, а не «на
розсуд моделі». Скарги передаються людині завжди: у тестах модель інколи бралася
сама відповідати на «прийшов бракований товар».

```mermaid
flowchart TB
    IN["Запитання + топ-5 фрагментів"] --> C1{"Це скарга,<br/>повернення, брак?"}
    C1 -- "так" --> ESC1["Менеджер<br/>рішення в коді"]
    C1 -- "ні" --> C2{"Найкраща близькість<br/>≥ 0.30?"}
    C2 -- "ні" --> ESC2["Менеджер<br/>у базі нічого немає"]
    C2 -- "так" --> C3{"Тип запиту<br/>про товар?"}
    C3 -- "так" --> PR["Підбір карток товарів<br/>до 3 штук"]
    C3 -- "ні" --> GEN
    PR --> GEN["LLM<br/>промпт мовою відповіді"]
    GEN --> C4{"Модель сама просить<br/>передати людині?"}
    C4 -- "так" --> ESC3["Менеджер<br/>медичне або комерційне"]
    C4 -- "ні" --> OK["Відповідь<br/>+ джерела + впевненість"]
```

| Поведінка | Коли |
|---|---|
| Відповідає сам | Склад, застосування, протоколи, семінари — усе, що прямо описано в матеріалах бренду |
| Передає менеджеру | Скарги, ціни й опт, медичні протипоказання |
| Каже «не знаю» | Питання поза базою знань — вигадана відповідь коштує бренду дорожче за відсутню |

Якщо LLM недоступний, відповідь формується екстрактивно з фрагментів — сервіс
не падає, лише деградує.

---

## 5. Чому картки товарів шукаються окремо

Сторінки товарів короткі — назва, склад, кілька рядків опису. У загальному пошуку
вони програють розлогим оглядовим сторінкам категорій. Тому для карток працює
окремий пошук з іншими вагами: збіг за назвою важить `×3` проти `×1` за змістом.

Збіг за назвою рахується триграмами, а не словником: українська й російська —
флективні мови, і «для тіла» не збігається з «тілом» дослівно. На реальному
прикладі справжній збіг дав `0.50`, а шум — `0.167`, звідси поріг `0.45`.

```mermaid
flowchart LR
    Q["Запитання"] --> ST["Прибрати стоп-слова<br/>«який», «для», «є»"]
    ST --> A["Триграмна схожість<br/>з назвою товару<br/>поріг 0.45"]
    ST --> B["Векторна близькість<br/>серед is_product"]
    A --> M{{"Зважений RRF<br/>назва ×3 · зміст ×1"}}
    B --> M
    M --> CARD["До 3 карток:<br/>фото · ціна · посилання"]
    CARD --> CTX["У контекст моделі<br/>ДО генерації"]
    CTX --> ANS["Текст відповіді<br/>узгоджений з картками"]
```

> **Ключова деталь — порядок.** Товари шукаються *до* звернення до моделі й
> потрапляють у той самий контекст. Інакше виходив розрив, який ми спіймали в
> тестах: під відповіддю показувалися картки пептидних засобів, а текст над ними
> стверджував, що інформації немає.

---

## 6. Межа периметра: що залишає сервер

Назовні йде тільки саме запитання разом із п'ятьма знайденими фрагментами.
База знань, історія розмов і переклади інтерфейсу залишаються в PostgreSQL,
доступному лише з `127.0.0.1`.

```mermaid
flowchart LR
    subgraph IN["Ваш сервер"]
        direction TB
        I1[("База знань<br/>319 фрагментів")]
        I2[("Історія розмов")]
        I3[("Переклади інтерфейсу")]
        I4["Модель ембедінгів<br/>ONNX, без мережі"]
        I5["RAG API<br/>127.0.0.1:8100"]
        I1 --- I5
        I2 --- I5
        I3 --- I5
        I4 --- I5
    end

    subgraph OUT["Зовнішній провайдер"]
        direction TB
        O1["LLM-провайдер"]
    end

    I5 -- "запитання + 5 фрагментів<br/>+ до 6 попередніх реплік" --> O1
    O1 -- "тільки текст відповіді" --> I5
```

Провайдер замінюваний — звернення до нього йде через одну функцію, тож перехід
на локальну модель не зачіпає решту системи.

Детальніше про потоки даних і модель загроз — у [SECURITY.md](SECURITY.md).

---

## 7. Вимірювання: MiniLM проти bge-m3

Щоб вибір моделі ембедінгів не був справою смаку, ми зміряли обидві на 12 реальних
питаннях (10 демо-питань українською + по одному російською та англійською для
перевірки крос-мовності). Кожна модель шукала по тих самих 319 фрагментах;
релевантність топ-5 розмічав LLM-суддя, який **не знав**, яка модель що знайшла.

| Конфігурація | P@5 | MRR | Питань хоч з одним влучанням |
|---|---:|---:|---:|
| лише вектор · MiniLM (384) | 0.250 | 0.417 | 7/12 |
| лише вектор · bge-m3 (1024) | 0.150 | 0.278 | 6/12 |
| гібрид + RRF · MiniLM | 0.200 | 0.357 | **8/12** |
| гібрид + RRF · bge-m3 | 0.183 | 0.315 | 7/12 |

Затримка ембедінгу одного запиту: MiniLM ≈ 140 мс локально, bge-m3 ≈ 144 мс через
TEI-сервіс. Розмір індексу в bge-m3 більший у 2,7 раза (1024 виміри проти 384).

**Висновок: підстав переходити на bge-m3 немає.** Вона не виграє в якості на цій
базі знань, потребує окремого сервісу й утричі більшого індексу. Різниця між
моделями на вибірці з 12 питань лежить у межах шуму — і це головне, що показав
експеримент: **вузьке місце не модель, а сама база знань**.

Обидві моделі провалюються на тих самих питаннях («які пептидні засоби»,
«догляд за тілом», «які семінари»), бо релевантного змісту в базі просто немає:
сторінка «Розклад семінарів» складається з маркетингового тексту без переліку
семінарів, а сторінки товарів дають по одному короткому фрагменту й тонуть серед
оглядових сторінок бренду. Жодна модель ембедінгів це не виправить — потрібні
матеріали від замовника (PIF, семінарські презентації, картки товарів зі складом).

Тому пріоритет для продакшену такий: спершу наповнення бази, далі перевірка
чанкування під короткі товарні сторінки, і тільки потім — вибір моделі ембедінгів.
